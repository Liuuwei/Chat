#pragma once

enum MessageType {
    LOGIN = 1, 
    LOGIN_ACK = 2, 
    REGIS = 3,  
    REGIS_ACK = 4,
    ONE_CHAT = 5,
    ADD_FRI = 6,
    CREATE_GROUP = 7,
    JOIN_GROUP = 8,
    GROUP_CHAT = 9,
};
